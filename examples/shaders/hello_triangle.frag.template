#version 450
#extension GL_ARB_separate_shader_objects : enable

layout(location = LOCATION_FRAGCOLOR) in vec3 fragColor;

layout(location = LOCATION_OUTCOLOR) out vec4 outColor;

void main() {
    outColor = vec4(fragColor, 1.0);
}