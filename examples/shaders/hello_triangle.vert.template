#version 450

layout(location = LOCATION_POSITION ) in vec2 position;
layout(location = LOCATION_COLOR    ) in vec3 color;
layout(location = LOCATION_FRAGCOLOR) out vec3 fragColor;

void main() {
    gl_Position = vec4(position, 0.0, 1.0);
    fragColor = color;
}